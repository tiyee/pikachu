database:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  database: "test_db"
  server_id: 100
  charset: "utf8mb4"

log:
  level: "info"
  format: "text"

server:
  enabled: true
  port: 8080
  path: "/health"

dispatcher:
  worker_count: 5
  queue_size: 100
  timeout: 30s
  max_retries: 3
  retry_base_delay: 10s

monitor:
  event_queue_size: 1000
  event_queue_timeout: 5s

callback_host: ""
